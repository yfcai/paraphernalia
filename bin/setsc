#!/bin/sh

if [ $# -eq 1 ]
then
  if [[ "$1" = /* ]]
  then
    export SCDIR=$1
  else
    export SCDIR=`ls -d "$PWD/$1"`
  fi
elif [ $# -eq 0 ]
then
  export SCDIR=$PWD
else
 echo "Usage: setsc <base dir for sc>"
fi
